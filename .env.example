# ─── Core ────────────────────────────────────────────────────────────────────
PORT=8787
API_BASE_URL=https://api.rendley.com/v1
APP_BASE_URL=https://app.rendley.com

# ─── Browser driver ──────────────────────────────────────────────────────────
# local = in-process Playwright Chrome; remote = Cloudflare browser worker.
BROWSER_MODE=local
HEADLESS=true
USE_CHROME_CHANNEL=true
CPU_ONLY=false

# ─── Logging & telemetry ─────────────────────────────────────────────────────
LOG_LEVEL=info
# Ship logs to PostHog Logs (requires POSTHOG_API_KEY)
LOGS_ENABLED=false


# ─── OAuth ───────────────────────────────────────────────────────────────────
MCP_PUBLIC_URL=https://mcp.rendley.com
# OPENAI_APPS_CHALLENGE_TOKEN=


# ─── Capacity tuning (optional) ──────────────────────────────────────────────
# QUEUE_CONCURRENCY=120        # max agent runs launching a browser at once
# QUEUE_MAX_QUEUED=2000        # global cap on edits in flight; the only hard reject
# BROWSER_RECYCLE_AFTER=8
# AGENT_TIMEOUT_MS=480000
# CHROMIUM_JS_HEAP_MB=512
